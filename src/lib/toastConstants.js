/**
 * Dose confirmation toast: minimum time the message stays at full opacity (5s).
 * Dismiss + CSS animation total = `TOAST_DURATION_MS + 500` ms (enter/exit fades).
 */
export const TOAST_DURATION_MS = 5000;
